{
  "name": "@oneact/orchestrator",
  "version": "0.1.0",
  "type": "module",
  "license": "Apache-2.0",
  "description": "一幕 OneAct · 多 Agent 协同编排层（混合协作 + 质量门禁 + 自愈管线）",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@oneact/ai": "workspace:*",
    "@oneact/skills": "workspace:*",
    "@oneact/schema": "workspace:*",
    "@oneact/layouts": "workspace:*"
  }
}
