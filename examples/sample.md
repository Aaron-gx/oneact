# Markdown 导入演示

## 第一页 · 简介

这是一幕 OneAct 的 Markdown 导入功能（md2act）。普通段落会变成 paragraph 组件。

- 要点一：Markdown 列表项
- 要点二：连续列表合并为一个 bullet-list
- 要点三：自动垂直堆叠

## 第二页 · 引用与图片

> 这是一句引用，渲染为强调段落。

![示意图](https://example.com/diagram.png)

图片由 Markdown 语法 `![alt](src)` 转换，自动带上 alt。

## 第三页 · 代码块

下面是一个代码块：

```
function hello() {
  console.log("OneAct");
}
```

结束。
