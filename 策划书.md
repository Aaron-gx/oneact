# 一幕 OneAct · AI 原生演示框架策划书

> 版本 v1.3 · 2026-08-01 设计定稿 / 2026-08-03 实现落地 · 状态：**核心已实现**（命名：一幕 OneAct；9 项决策已拍板；已吸收两轮外部架构审查：图表语义层、slot 坐标规范、**两级生成模式**、离屏真实测量校验、逃逸块安全沙箱、assets 目录约定、表格规则、a11y 字段、黄金测试集、多模型分档）。**v0.1–v1 + 多 Agent 编排 + Skill 系统均已实现，290 tests 通过——见文末「附录 B：实现现状」。**

---

## 1. 一句话定位

一套**开放的、AI 原生的演示文档格式 + 渲染运行时**——PPT 界的 HTML + 浏览器。

AI 用 JSON 写幻灯片，框架负责高保真渲染，人像用 WPS 一样逐页预览，并可用自然语言指挥 AI 局部修改任意页面元素。

## 2. 为什么有机会（市场空档）

| 现有方案      | 代表                        | 局限                |
| --------- | ------------------------- | ----------------- |
| 网页演示框架    | reveal.js / Slidev / Marp | 服务"给人写代码"，非 AI 友好 |
| AI PPT 产品 | Gamma / Kimi PPT / 美图     | 格式封闭、不可编程、不可本地嵌入  |

**空档：以 AI 为一等公民的"开放格式 + 运行时"。** AI 时代演示文档还没有事实标准，先占位者定义标准。

## 3. 核心架构（四层）

1. **AI 生成层**：任意 LLM / Agent + 框架说明书（组件清单、JSON Schema、设计规范、MCP 工具）
2. **内容层**：slides.json —— 页面 = 版式 + 组件树，全部元素 id 可寻址
3. **渲染层**：Web 运行时（1280×720 固定画布 + scale 适配、组件库、主题 token、动画引擎、校验器）
4. **交互层**：缩略图栏 / 画布点选 / 属性面板 / 播放 / 导出

**协作回路**：人指定某页某元素（"第 3 页图表换成饼图"）→ AI 只重写该片段 → 校验器检查 → 重渲染。其余页面不动。

## 4. 关键设计决策

### 4.1 AI 的语言：slides.json

- JSON Schema 声明式组件树为主：可校验、可程序化编辑、AI 生成质量稳定
- 逃逸块兜底：`custom-html` / `custom-svg` 组件，保证表现力上限 = 浏览器上限

### 4.2 画布模型：固定逻辑坐标 + 等比缩放

- 固定逻辑分辨率 1280×720（16:9），元素绝对定位
- **自适应 = 整个画布按窗口等比 scale + 居中**：任何屏幕 / 分辨率 / 浏览器下布局完全一致、绝不重排（投影仪模型：画面缩放，构图不变）
- 为什么不用流式响应式：演示文稿要求同一份 deck 在任何设备上看起来**完全相同**；流式重排会让排版随屏幕变化，是演示场景的灾难（PPT / Keynote / Google Slides / reveal.js 全部同此模型）
- 页面尺寸可配置（16:9 / 4:3 / 16:10），单份 deck 内固定
- AI 给坐标时所见即所得；导出 PDF / 图片不变形

### 4.3 组件库 + 版式原语

- 组件：标题 / 段落 / 列表 / 图片 / 图表 / 表格 / 形状 / 图标 / 公式 / 音视频 / 逃逸块
- 版式：标题页、目录页、左右分栏、三卡片、大图页、数据页、引用页、结束页……
- **AI 先选版式再填内容**，不允许自由发挥坐标

**每种版式在 layouts 包中携带 slot 坐标模板**——AI 只需"选版式 → 选 slot → 填内容"，零坐标计算：

```json
{
  "layout": "two-column",
  "slots": {
    "title":    { "rect": [64, 48, 1152, 60],   "accepts": ["heading"] },
    "subtitle": { "rect": [64, 120, 700, 30],   "accepts": ["text"] },
    "left":     { "rect": [64, 180, 560, 480],  "accepts": ["bullet-list", "text", "image"] },
    "right":    { "rect": [656, 180, 560, 480], "accepts": ["chart", "image", "table"] }
  }
}
```

- 存储时坐标取 slot 值（AI 微调时可覆盖 rect）；校验器加软警告"元素超出其 slot 范围"
- `accepts` 约束组件归位，校验器可硬校验

**两级生成模式（坐标问题的根本解）**——LLM 的空间推理是模式匹配而非真计算，因此：

- **约束模式（默认，约 90% 页面）**：AI 只做"内容 → slot"的语义归类，坐标取 slot 模板值，**AI 完全不写像素数字**；slot 支持 `auto` 高度语义（如要点列表：行数 × 行高，自 slot 顶部起排，布局引擎编译时展开为具体 rect）
- **自由坐标模式（逃逸层专用）**：仅炫技页 / 自定义布局由 AI 直接写 rect，走"生成后校验 + 人工微调"路径，不指望一次到位
- 与 4.1 的"组件层 / 逃逸层"双层设计完美同构：约束模式 = 组件层，自由坐标 = 逃逸层
- 演进：v0 用静态 slot 模板；layouts 包渐进增强为"内容感知布局引擎"（简化 flexbox），接口不变

**组件优先级**（按"AI 最常生成的内容"排序）：

| 优先级 | 组件 | 理由 |
| ------ | ---- | ---- |
| P0（v0 必须） | heading / paragraph / bullet-list / image / chart | 构成 90% 的页面 |
| P1（v1） | table / shape / icon | 增强表现力 |
| P2（v2） | formula / video-audio / 逃逸块（custom-html/svg） | 学术与多媒体；逃逸块是"炫技页"基础且实现简单，故提前 |
| P3（v3） | 社区组件 | 生态 |

### 4.4 校验器（机器可校验性 = 框架的灵魂）

- 自动检查：元素越界、重叠、字号过小、对比度不足、文本溢出容器
- 不通过 → 带错误定位信息打回 AI 自我修复

**规则清单（两级）**：

| 级别 | 规则 | 处理 |
| ---- | ---- | ---- |
| 硬错误 | 元素超出画布边界 | 打回，必须修复 |
| 硬错误 | id 重复 / 未知组件类型 / 必填字段缺失 | 打回，必须修复 |
| 软警告 | 元素重叠面积 > 20% | 提示，AI 应修复 |
| 软警告 | 正文字号 < 14px / 标题 < 24px | 提示 |
| 软警告 | 文字与背景对比度 < 4.5 : 1 | 提示 |
| 软警告 | 文本估算溢出（字数 × 字号系数 > 容器容量） | 提示，附溢出量 |
| 软警告 | 单页元素 > 12 个（信息过载） | 提示 |

**测量方式（v0 技术决策）**：公式估算只作渲染前快速预检；**终审用离屏真实测量**——校验器将页面 DOM 离屏渲染，以 `scrollHeight > clientHeight` / `getBoundingClientRect()` 读取真实溢出与重叠，消除中英文混排、加粗、padding 带来的估算误差。成本仅一次 headless 渲染，准确度数量级提升。

### 4.5 可寻址性

- 每页 `pageId`、每元素 `id`，局部重写不触碰其他页面
- 这是"像 WPS 一样编辑"能成立的前提

### 4.6 动画系统：声明式原语，兼容 Office 动画体系

**原则：命名与行为对标 PowerPoint / WPS 的动画心智**——用户和 AI 都不需要学习新概念，主流那套直接拿来兼容；但底层是自创的声明式模型（别人没有为 AI 设计的动画 schema = 差异化壁垒）。

**四类动画原语 + 编排**（schema 内声明，校验器可检查，pptx 导出可降级）：

| 类别 | 兼容预设（中英双名） |
| ---- | ------------------ |
| 进入 entrance | 出现 appear · 淡出 fade-in · 飞入 fly-in-{dir} · 浮入 float-in · 擦除 wipe-{dir} · 劈裂 split · 缩放 zoom-in · 轮子 wheel · 随机线条 random-bars |
| 强调 emphasis | 脉冲 pulse · 跷跷板 teeter · 放大缩小 grow-shrink · 变色 color-pulse · 闪烁 flash · 陀螺旋转 spin |
| 退出 exit | 淡出 fade-out · 飞出 fly-out-{dir} · 消失 disappear · 擦除 wipe-out-{dir} |
| 路径 motion（后期） | 直线 line · 弧形 arc · 自定义 custom |

**页面切换 transition**：淡入淡出 fade · 推入 push-{dir} · 擦除 wipe-{dir} · 覆盖 cover-{dir} · 揭开 uncover · 百叶窗 blinds · 棋盘 checkerboard · 缩放 zoom · **平滑 morph**（FLIP 实现，对标 PPT 平滑切换）

**编排模型**：顺序 sequence / 同时 with / 错落 stagger；参数仅 `duration` / `delay` / `easing` 三项。

AI 写法示例：`{"anim": "fly-in-left", "duration": 600, "stagger": 120}`——声明式，几乎不可能写错；自由时间轴动画后期以"高级动画块"（逃逸层）加入。

### 4.7 slides.json 格式草案（格式 = 核心资产）

设计原则：**AI 写得对，人也读得懂**——格式即文档。

```json
{
  "formatVersion": 1,
  "meta": { "title": "产品发布演示", "theme": "yuanshan-blue", "size": "16:9" },
  "pages": [
    {
      "id": "p3",
      "layout": "two-column",
      "transition": { "name": "push-left", "duration": 500 },
      "background": { "color": "#FFFFFF" },
      "notes": "这一页讲市场机会，重点强调空档",
      "elements": [
        {
          "id": "el-1", "type": "heading",
          "rect": [64, 48, 700, 60],
          "props": { "text": "为什么是现在", "level": 1 }
        },
        {
          "id": "el-3", "type": "bullet-list",
          "rect": [64, 218, 572, 318],
          "props": {
            "items": [
              [{ "text": "两端都有成熟产品，", "bold": true }, { "text": "中间是空的" }],
              [{ "text": "浏览器渲染天花板已超 Office" }]
            ]
          },
          "anim": { "name": "fly-in-left", "duration": 600, "stagger": 120 }
        }
      ]
    }
  ]
}
```

关键约定：

- **rect = [x, y, w, h]**：1280×720 逻辑像素，四元数组紧凑，AI 易写不易错
- **富文本 = runs 数组**：一段文字由若干片段组成，每片可带 bold / italic / color，支持混排
- **版式是"编译时脚手架"**：`layout` 只记录"参考了什么版式"（供日后"换版式"操作），坐标永远是绝对值——AI 可微调、编辑器可拖拽、校验器好计算
- **z 序 = 数组顺序**：后面的元素在上层，不显式存 z-index
- **theme 只存名字**：token 由主题包解析，换主题不动内容
- **无障碍内建**：image 组件 `alt` 必填（缺省触发软警告）、chart 组件带 `summary` 文字摘要字段——格式冻结前加字段零成本，事后加要走迁移器

### 4.8 主题 design tokens

每套主题 = 一组 token，运行时注入 CSS 变量：

| token 组 | 内容 |
| -------- | ---- |
| colors | primary / accent / text / text-secondary / bg / surface / border |
| fonts | heading / body / number（中西文回退栈） |
| fontSizes | 阶梯 44 / 32 / 24 / 18 / 14 / 12（校验器字号下限的依据） |
| radius / shadow / spacing | 圆角、阴影档、间距基数 |

内置主题（v0 先 1 套，v1 补齐 3 套）：**远山蓝**（商务，默认）· **墨绿**（学术）· **暖橙**（活力）。主题即插件，社区可发布。

### 4.9 图表语义层（关键补丁：AI 不写 ECharts option）

外部架构审查发现的硬缺口：若让 AI 直接写 ECharts option（嵌套深、字段多），必翻车且颜色硬编码破坏主题。改为**语义层**——AI 只写"类型 + 数据"：

```json
{
  "id": "el-4", "type": "chart",
  "rect": [656, 180, 560, 480],
  "props": {
    "chartType": "bar",
    "data": {
      "categories": ["2023", "2024", "2025", "2026"],
      "series": [{ "name": "关注度", "values": [1200, 3800, 9400, 27000] }]
    },
    "title": "AI 演示工具关注度趋势"
  }
}
```

**渲染层负责翻译成引擎配置**，三赢：

1. AI 只写极简语义，几乎不可能写错（且可校验：categories 与 values 长度一致等）
2. 颜色 / 字体全部走主题 token，换肤自动生效
3. pptx 导出可映射 **PPT 原生图表**（而非截图），降级损失最小

实现路径：v0.1 手绘 SVG 渲染 bar / pie / line 三图（零依赖、约 200 行）；v0.2 起翻译到 ECharts（按需加载）。

### 4.10 表格组件规则（固定画布模型下最难的组件，提前定死）

- **行高内容驱动**：行高 = 字号 × 行距 + padding；整表高度 = Σ 各行（表头固定）
- `rect` 的 h 语义为**最大高度**：内容超出即触发校验器软警告（AI 应压缩行数或字号），不做表内滚动（演示场景滚动是反模式）
- 列宽用**比例**声明（`columns: [2, 1, 1]`），随 rect 宽度自适应；单元格文本溢出按通用文本规则测量

### 4.11 逃逸块安全沙箱（发布红线）

打开"陌生人分享的 .act / 自包含 HTML"是核心分发场景，逃逸块（custom-html / svg）必须默认不可信：

- **渲染隔离**：`iframe sandbox`（默认禁脚本、禁同源、禁表单）+ 严格 CSP；确需脚本的逃逸块须用户显式授权
- **解析消毒**：加载时对自定义标记做 DOMPurify 白名单过滤
- **静态扫描**：校验器对逃逸块内容做可疑 script / 外链 / 事件属性扫描，命中即隔离并提示

## 5. 渲染能力承诺

**结论：Web 渲染天花板 ≥ Office**（Chromium 渲染引擎），并多出交互组件、实时数据两个 PPT 不具备的维度。

| 能力                               | PowerPoint | Web 运行时              |
| -------------------------------- | ---------- | -------------------- |
| 文字排版 / 矢量图形 / 滤镜蒙版 / 图表 / 3D 音视频 | 支持         | 支持                   |
| 页面平滑切换（Morph）                    | 支持         | 需开发（FLIP 技术）         |
| 元素路径动画                           | 支持         | 需开发（GSAP MotionPath） |
| 交互组件 / 实时数据                      | 弱或无        | 支持                   |

**双层兑现承诺**：组件层保 90% 常规页面稳定；逃逸层保 10% 高难度页面可达任意视觉效果。

**字体策略**：默认系统字体栈（中文友好、零加载成本），主题可指定 Web 字体（子集化加载）。

## 6. 产品形态：一份核心 · 四种形态

| 形态           | 面向   | 说明                                       |
| ------------ | ---- | ---------------------------------------- |
| npm SDK      | 开发者  | `<DeckPlayer deck={json} />` 嵌入任何 Web 产品 |
| runtime.html | 所有人  | 单文件播放器，双击播放 .act；导出时可自包含打包成单 HTML 分发    |
| Web 应用       | 终端用户 | 在线编辑器 + AI 对话生成                          |
| 桌面应用         | 终端用户 | Tauri 壳（非另写一套），关联 .act 文件，本地离线          |

### 6.1 编辑器 UI 布局（界面稿见 `design/editor-mockup.html`）

类 WPS 三栏结构，每个区域都对应一项框架能力：

- **顶栏**：文件名（.act）· 主题切换 · AI 生成入口 · 导出（PDF / .pptx / .act）· 播放
- **左栏**：页面缩略图列表，当前页高亮，可拖拽排序，底部"新建页面（或让 AI 续写）"
- **中栏**：1280×720 画布随窗口等比缩放；点击元素即选中，选中框标注 `组件名 · #id`（可寻址性可视化）
- **右栏**：属性面板（位置尺寸 / 文本 / 动画——动画下拉直接列出 Office 兼容预设）+ 底部"让 AI 改这一页"输入框（只重写当前 page 片段）
- **底栏**：页码 · 校验器实时状态（无越界 / 无重叠 / 字号达标）· 缩放比 · formatVersion

## 7. 开放接口（API 三层）

1. **JS/TS SDK** —— 开发者同步嵌入（开源后主要集成方式）
2. **HTTP API** —— 自托管渲染服务（POST slides.json → PDF/PNG），服务非 JS 环境
3. **LLM 接入** —— skill 说明书 + JSON Schema + MCP server，任何 Agent 可用

**模型 key 用户自配**：ai 包做 provider 适配层（OpenAI 兼容协议 / Anthropic / Gemini / 国产模型），key 只存用户本地，不经过任何服务器。

### 7.1 AI 生成协议（框架说明书核心）

1. **两段式生成**：AI 先输出大纲（每页 = 版式 + 标题 + 要点摘要）→ 用户确认 → 再逐页生成完整 JSON。避免"一口气生成 20 页全跑偏"。
2. **局部修改 = 整页重写**：输入 = 当前 page JSON + 用户指令，输出 = 完整新 page JSON。**不做 diff 合并**——模型输出整页比处理重叠 diff 可靠得多，代价只是多几百 token。
3. **生成后必过校验器**：不过 → 带错误定位（哪个元素、违反哪条规则、超出多少）自动重试，默认上限 2 次；仍不过则如实告知用户，不静默带病交付。
4. **说明书结构**：角色 → 格式规范 → 组件清单（props + 适用场景）→ 版式坐标规范 → 设计规范（单页信息量、字号阶梯、留白规则）→ 输出纪律（仅 JSON）→ 自检清单（含**负面示例**：常见错误写法 vs 正确写法对照——模型从反例中学得最快）。
5. **流式按页预览**：逐页生成时，每生成一页立即校验并渲染该页（不等全部完成）——用户实时看到 PPT"长出来"。
6. **图片来源策略**：image 组件 `src` 支持 URL / base64 / 占位符（`"placeholder:产品截图"` → 渲染为灰色占位块 + 文字标注）。AI 生成默认用占位块或指定图床 URL；AI 生图走独立 skill（用户自配图像模型 key），不进核心协议。
7. **多模型分档策略**：不同模型的结构化生成严谨度差异大。说明书按模型档位给不同强度 few-shot——弱模型更多示例 + **强制约束模式（禁自由坐标）**，强模型放开；黄金测试集须在至少 3 档模型上跑分，避免"用户选了弱模型却怪框架不稳"。

## 8. 双格式导出

| 格式    | 定位       | 策略                                                                          |
| ----- | -------- | --------------------------------------------------------------------------- |
| .pptx | 降级导出     | 静态页直接映射；动画/交互转静态帧或终态；附**降级报告**（明示丢失项，不偷偷丢东西）；PptxGenJS 实现                   |
| .act | 专有格式，全保真 | slides.json + 媒体资源 + formatVersion 打包；可进一步打成**自包含单 HTML**，发给任何人双击即播，动画交互全保真 |

**.act 物理格式**：v0 = 单文件 UTF-8 JSON + **同名 assets 目录约定**（`demo.act` 配 `demo.assets/`，图片 src 用相对路径）——文本 git 可 diff；图片**不做 base64 内联**（base64 会让 diff 爆炸：改一个像素整段编码全变），base64 仅用于自包含单 HTML 分发产物（分发场景无需 diff）。v2 增加 `.actpack`（zip 包：content.json + assets/），与目录约定天然同构、零迁移成本。

## 9. 开源架构

### monorepo（pnpm）

```
oneact/
├── packages/
│   ├── schema/          格式定义 + TS 类型 + JSON Schema + 校验器（平台无关）
│   ├── core/            渲染引擎 + 组件注册表 + 主题引擎 + 动画引擎
│   ├── components/      内置组件库（文本/图表/表格/形状/SVG逃逸块…）
│   ├── layouts/         版式原语（标题页/分栏/卡片/数据页…）
│   ├── player/          播放器（翻页/Morph/演讲者视图）
│   ├── editor/          编辑器（缩略图栏/点选/属性面板）
│   ├── ai/              模型适配 + 生成协议 + 局部重写 + 自校验循环
│   ├── exporter-pptx/   pptx 降级导出
│   ├── exporter-deck/   .act 打包与解包
│   └── cli/             init / validate / export / serve
├── apps/
│   ├── web/             在线应用
│   └── desktop/         Tauri 壳
└── skills/              官方 AI Skill（生成说明书 + MCP server）
```

### 两条铁律

1. **schema 版本化**：每份 deck 携带 `formatVersion`，运行时向后兼容；格式永不 breaking change，只新增迁移器——**链式纯函数** `migrate(deck, from, to)`（v1→v2→v3 逐级应用）；旧 runtime 遇新格式给出明确提示，不静默渲染错误。格式一旦发布即冻结。
2. **核心框架无关**：渲染核心不绑 React，Web Components 封装 + 官方 React / Vue 绑定，最大化接入面。

## 10. 插件与 Skill 生态

- **五类注册点**：组件 / 版式 / 主题 / 导出器 / AI Skill
- **AI Skill = 生成说明书 + 领域组件包 + 专属校验规则**
  - 例：咨询风 skill（麦肯锡配色 + 瀑布图组件 + "每页必须有 so-what 标题"校验规则）
  - 例：学术海报 skill、数据周报 skill、产品发布 skill
- v3 开放 skill 市场，社区可发布

> **已实现（2026-08-03，`@oneact/skills` 包）**：Skill 落地为**三正交维度可叠加**的场景知识包——
> - **structure**（结构骨架）/ **style**（视觉风格）/ **domain**（行业知识），每维单选，`composeSkills` 叠加注入 `buildSpec` §12 + `applySkill` 仲裁写回 Brief。
> - **9 个内置 skill**：路演 BP / 工作汇报 / 学术答辩 / 产品发布会（structure）· Apple 极简 / 咨询专业 / 科技发布会（style）· 金融 / 医疗（domain）。
> - **用户自导入**：Markdown frontmatter 格式（`parseFrontmatter`），经 Web 编辑器 / skill 管理器导入存 localStorage；MCP 经 `oneact_list_skills` + `skillIds` 参数。
> - **仲裁铁律**：用户显式输入 > skill > 原推断（`applySkill` 唯一仲裁点；`composeSkills` 只收集不决策）。

## 11. 技术选型

TypeScript · pnpm monorepo · Vite · ECharts（图表，按需加载）· GSAP（动画，MIT）· PptxGenJS（pptx 导出）· KaTeX（公式）· Tauri（桌面）· Changesets（版本管理）

## 12. 演进路线

> 截至 2026-08-03，v0.1–v1 + 多 Agent 编排 + Skill 均已实现（**290 tests / 24 文件全通过，0 type errors**）：

| 版本 | 状态 | 目标 | 内容 |
| -- | --- | ---- | --- |
| v0.1 | ✅ | **30 秒可体验** | 单文件 player（runtime.html）+ schema 全量 + 校验器 + **25 元素** + **20 版式** + **12 主题** + 动画 + 手绘 SVG 图表（bar/line/pie/doughnut/area） |
| v0.2 | ✅ | 核心闭环 | CLI（validate/init/export/serve/http）+ md2act + HTTP 渲染服务 + pptx 导入文本提取 + 离屏测量 |
| v0.3 | ✅ | AI 闭环 | 两段式生成 + 流式按页预览 + **黄金测试集跑分（多档模型）** + 多模型分档路由 + token 预算 + prompt A/B + 安全护栏 |
| v1 | ✅ | **开源就绪** | MCP server（6 工具）+ Web 编辑器（三栏 + AI 局部重写 + 灵动岛问卷）+ pptx 降级导出（25 元素 + 降级报告） |
| **+** | ✅ | 多 Agent 编排 | Director/Generator/Inspector/Healer 四 Agent + G1/G2/G3 三门禁 + Session（cancel/pause）+ 事件总线 |
| **+** | ✅ | Skill 系统 | 三维度（结构/风格/行业）+ 9 内置 skill + 用户 Markdown 自导入 + topic 自动推荐 |
| v2 | ⬜ | 编辑器增强 | 桌面端编译发布、协作（CRDT）、演讲者视图双屏 |
| v3 | ⬜ | 生态 | 插件/skill 市场、HTTP 渲染服务增强 |

> 说明：v0.1 与 monorepo 不冲突——**工程结构是 monorepo，首个交付物是单文件 player**（packages/player 的构建产物即 runtime.html），两者同日开始。

## 13. 风险与对策

| 风险        | 对策                                |
| --------- | --------------------------------- |
| AI 排版翻车   | 版式原语 + 校验器 + 自动修复回路               |
| 大厂抢先定义格式  | 快速开源占位；schema 小而开放，接入成本极低         |
| 社区冷启动     | 炫技 demo 页传播 + 主流 Agent 工具链集成（MCP） |
| pptx 导出失真 | 明确定位"降级导出" + 降级报告，不承诺像素级          |
| 中文字体体积    | 系统字体栈默认 + Web 字体子集化               |
| 生态组件质量参差  | 官方认证机制 + 校验器强制约束                  |

## 14. 决策记录（2026-08-01 头脑风暴定稿）

| # | 问题 | 决策 |
| - | ---- | ---- |
| 1 | 第一优先级用户 | **开发者 / Agent 生态优先**，兼顾终端使用者体验（SDK 与格式先行；播放与演示体验不妥协） |
| 2 | 项目命名 | **已定稿：中文「一幕」+ 英文 OneAct**（独幕剧 one-act play 双关）；格式扩展名 `.act`；占用实况与注册方案见附录 |
| 3 | 生成入口 | **双轨并行**：内置生成（用户自配 API key，provider 适配层，key 仅存本地）+ 外部 Agent（skill 说明书 / MCP server）；开放优先，不闭关锁国 |
| 4 | 动画 schema | **自建声明式动画原语**——enter / emphasis / exit / transition 四类 + sequence / stagger 编排，作为框架级差异化能力（市面上没有为 AI 设计的动画模型）；自由时间轴后期以高级块形式加入 |
| 5 | 许可证 | **Apache-2.0**（含专利授权，大公司敢用） |
| 6 | 商业化边界 | 核心 100% 开源；未来云托管 / 协作服务另起闭源仓库（默认成立，待最终确认） |
| 7 | v0 技术起点 | 直接 pnpm monorepo，骨架即开源结构；第一刀切 schema + core + player 三包 |
| 8 | 图表引擎 | ECharts（按需加载）；chart 组件抽象引擎接口，允许插件替换引擎 |
| 9 | 演讲者视图 | schema 的 `notes` 字段 v0 即入格式；演讲者视图 UI（备注 / 计时 / 双屏）进 v2 |

### 有意留白（本版不展开，避免过度设计）

以下事项方向已定但细节刻意留空，到对应阶段再细化：

| 事项 | 细化时机 |
| ---- | -------- |
| 编辑器微交互（拖拽手感、右键菜单、快捷键表） | v2 编辑器开发前 |
| pptx 动画降级映射总表（每种动画 → 静态策略） | v2 导出器开发前 |
| 实时协作（CRDT 选型、冲突合并） | v3 |
| 插件市场运营细则（审核、分成、签名） | v3 |
| 中文标点挤压等高级排版特性 | 有用户反馈再议 |

---

## 附：命名定稿（2026-08-01 拍板）

**中文名「一幕」+ 英文名 OneAct**——英语戏剧术语"独幕剧"（one-act play），即"一幕"的国际通行对应词，全球发音无障碍。

**品牌术语体系**（名字带来的天然隐喻）：

| 概念 | 术语 | 隐喻 |
| ---- | ---- | ---- |
| 一份演示文件 | `.act` | 扩展名 |
| 一页幻灯片 | act / 一幕 | 翻页 = 换幕 |
| 整份 deck | full play / 一出戏 | |
| 播放器 | stage / 舞台 | runtime.html 即"舞台" |
| AI 生成一页 | 开演一幕 | |

**占用实况（2026-08-01 实测）**：

| 资产 | 状态 | 方案 |
| ---- | ---- | ---- |
| npm 包 `oneact` | 已被占（200） | 不用单词包名，全部走 scope |
| npm scope `@oneact` | 可用迹象（@oneact/core 404） | **立即注册 npm 用户/组织 `oneact`**；备选 `@oneact-sh` / `@oneactjs` |
| GitHub `oneact` / `one-act` | 均已被占（200） | **org 用 `oneact-sh`（已验证 404 可用）**；备选 `oneactjs` / `one-act-hq` |
| 域名 | 未查（需注册商） | 候选：`oneact.dev` / `oneact.show` / `oneactjs.com` |

**待办（需项目负责人本人操作）**：注册 npm 账号/组织 `oneact`、GitHub org `oneact-sh`、域名一个——开源发布前完成即可，但 npm 与 GitHub 建议尽快（防止被抢注）。

---

## 附录 B：实现现状（2026-08-03）

> 本节是设计落地的事后记录，与上文设计决策对照。核心地基 + AI 闭环 + 编辑器 + 多 Agent 编排 + Skill 均已实现。

### B.1 规模数据

| 维度 | 数值 |
| --- | --- |
| 元素类型 | **25**（13 基础 + 12 复合语义） |
| 内置主题 | **12** 套（完整 design tokens） |
| 版式 | **20** 套（含 slot 坐标模板） |
| 内置 skill | **9**（structure×4 + style×3 + domain×2） |
| CLI 命令 | **9** |
| MCP 工具 | **6**（含 oneact_list_skills） |
| 包 / 应用 | **12 packages + 2 apps** |
| 测试 | **290 tests / 24 文件** |

### B.2 包成熟度

| 包 | 状态 | 关键能力 |
| --- | --- | --- |
| schema | ✅ | 25 元素判别联合 + 校验器（两级规则）+ 链式迁移器 + JSON Schema + 12 主题 + Mood 类型（下沉至此，跨包单一事实源） |
| core | ✅ | 渲染引擎（输出 HTML 字符串，不绑 React）+ 组件注册表 + 主题/动画引擎 + 离屏测量 |
| components | ✅ | 25 组件全注册（手绘 SVG 图表 + 逃逸块 DOMPurify 消毒 + KaTeX 公式） |
| layouts | ✅ | 20 版式 + slot 规范 + auto 高度引擎 |
| ai | ✅ | provider 适配（OpenAI/Anthropic/Gemini/国产）+ 两段式生成 + 整页重写 + 校验自愈 + `applySkill` + 分档路由 + 预算 + SSE 流式 + prompt A/B + 安全护栏 + 黄金跑分 |
| skills | ✅ | 三维度类型 + `composeSkills` + `parseFrontmatter`/`serializeSkill` + 9 内置 |
| orchestrator | ✅ | 4 Agent（Director/Generator/Inspector/Healer）+ 3 门禁（G1/G2/G3）+ Session（cancel/pause/resume）+ 事件总线 + 结构化错误定位（ErrorLocation） |
| player | ✅ | 单文件 runtime.html（已构建） |
| exporter-deck | ✅ | .act 打包 + .actpack zip + 自包含单 HTML（双击即播） |
| exporter-pptx | ✅ | 25 元素全覆盖 + 降级报告（PptxGenJS） |
| mcp | ✅ | stdio server，6 工具，key 走环境变量（仅本地） |
| cli | ✅ | 9 命令（validate/init/from-md/from-pptx/export/serve/http/benchmark） |
| apps/web | ✅ | 三栏编辑器 + 灵动岛问卷 + skill 管理器 + AI 灵动岛可视化（5 Agent 协作）+ 双格式导出 + localStorage 文档持久化 |
| apps/desktop | 🟡 壳 | Tauri 配置骨架（.act 关联 + 复用 player runtime），需 Rust 工具链编译 |

### B.3 与原设计的偏差（落地时的演进）

- **主题**：设计写「v0 先 1 套，v1 补齐 3 套」→ 实际直接做到 **12 套**（token 机制成本低，一次到位）。
- **组件**：设计 P0–P3 分期 → 实际 **25 种一次到位**（含 12 复合语义组件：kpi/stat-grid/feature-card/timeline/process/comparison/callout/badge/divider/avatar/section-title/feature-list——AI 填语义字段即出整组视觉，是质量跃升的关键）。
- **版式**：设计列 8 种 → 实际 **20 种**（dashboard/comparison/process/timeline/stats-grid/feature-list/team/closing/hero/content 等高级版式提前）。
- **编排**：设计未单列多 Agent → 实际新增 `@oneact/orchestrator`（4 Agent + 3 门禁 + Session），从单模型串行升级为自治协作回路。
- **Skill**：设计 §10 设想「v3 市场」→ 实际**三维度 skill 系统**已落地（9 内置 + 用户 Markdown 导入 + Web/MCP/orchestrator 三入口透传），市场运营留 v3。
- **状态机**：设计了 FSM，v2 起门禁改为 Conductor 直接调用（`gates.ts` 注释明示「不再依赖 FSM 状态」），但 Session 状态机（running/paused/cancelled/completed/failed）仍完整保留。

### B.4 验证

```bash
pnpm test                   # 290 tests / 24 文件
pnpm typecheck              # 0 type errors（全 12 包）
node apps/web/build.mjs     # dist/editor.html ✓
node packages/mcp/build.mjs # dist/mcp.cjs ✓
```

**三条接入入口均已打通 skill**：Web 编辑器（briefing 选维度）/ MCP（`oneact_list_skills` + `skillIds`）/ orchestrator（`GenerateConfig.skills`）。skill 注入设计为**单一入口** `SpecOptions.skills`，无 skill 时 `buildSpec` 输出逐字节不变（零回归）。
